<?php
/**
 * Template Name: Portfolio stage
 * Template Post Type: page
 * The stage on its own, under the site chrome. Assign it to any page.
 */
get_header();
latent_render_stage();
latent_render_player();
get_footer();
